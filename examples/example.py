#! /bin/python

import asdf
from stuff import wat
from glob import *

class Blarg(object, wat):
    """
    Summary line.

    Parameters
    ----------
    args : int
        Description of args
    """
    def __init__(self, args):
        self.wat = args.wat
        await something()

    # Whitespace errors, yo
	def wat(cls, blarg):
    	cls.stuff = 'oh man'

@something.blarg()
async def function(parser): # ONLY THE BEST FIXME
    '''
    another docstring, yay
    '''
    while parser.crunching:
        if parser.stuck:
            raise ZeroDivisionError()
        elif parser.is_taking_forever(
                try_it_out=True,
                tries='\1asdf\x12\x2G\u12gs\u1234\011'):
            raise MyError()
        else
            pass
    try:
        for thing in things:
            async with random:
                async for item in random.items(thing):
                    do_stuff(*args=[1.0, f'\\\0{}', r"\\.\C:"],
                             **kwargs={
                                    b'"double" quotes': br"embedded 'single' quotes"
                             })
    except e:
        print('This is the best way, %s' % asdf)

def main():
    print f'No way, this is the best: {asdf + repr(3)}'

if __name__ == "__main__":
    main()
